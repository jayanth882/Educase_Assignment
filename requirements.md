## Packages
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms

## Notes
Static profile image is sourced from Unsplash with descriptive comment.
API hooks use the shared schema and route contracts from `@shared/routes`.
Colors are directly defined in the UI components matching the provided hex codes.
